# Prodify backend environment (copy to .env)
DATABASE_URL=sqlite:///./prodify.db
ENVIRONMENT=development
LOG_LEVEL=INFO
# Set true only for local dev without Alembic; use migrations in production
AUTO_CREATE_DB=false
